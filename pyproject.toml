[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "webthrifting"
version = "0.1.0"
description = "WebThrifting app"
readme = "README.md"
requires-python = ">=3.8"

[project.optional-dependencies]
dev = []

[tool.setuptools.packages.find]
where = ["."]
